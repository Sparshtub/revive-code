{
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "node -e \"const exe = process.platform === 'win32' ? '.venv\\\\Scripts\\\\python.exe' : '.venv/bin/python'; require('child_process').spawn(exe, ['-m', 'uvicorn', 'app.main:app', '--reload', '--reload-dir', 'app', '--port', '8000'], { stdio: 'inherit', shell: true, cwd: 'backend' })\"",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
